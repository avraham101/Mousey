<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Mousey"
             x:Class="Mousey.MainPage">

        <!-- Place new controls here -->
    <Grid RowSpacing="0" ColumnSpacing="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--Label For Knowing the Vector of Movment-->
        <Label Text="{Binding Label}" Grid.Row="0" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
        <!--The ip port Grid-->
        <Grid Grid.Row="1" Margin="2,0,2,2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Entry Grid.Column="0" Text="{Binding Ip}" HorizontalTextAlignment="Center" BackgroundColor="#66A3E0"/>
            <Entry Grid.Column="1" Text="{Binding Port}" HorizontalTextAlignment="Center" BackgroundColor="#66A3E0"/>
        </Grid>
        <!--The actual mouse Grid-->
        <Frame Grid.Row="2" Margin="2,0,2,2" OutlineColor="Black" BackgroundColor="LightGray" Padding="1" HasShadow="True" CornerRadius="10">
            <Grid RowSpacing="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="4*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <!--The buttons right click,roller,left click-->
                <Grid Grid.Row="0" ColumnSpacing="0" Margin="2,2,2,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="6*"/>
                        <!--Left-->
                        <ColumnDefinition Width="*"/>
                        <!--Roller-->
                        <ColumnDefinition Width="6*"/>
                        <!--Right-->
                    </Grid.ColumnDefinitions>
                    <!--Left-->
                    <Frame Grid.Column="0" OutlineColor="Black" BackgroundColor="Gray" Padding="3" HasShadow="True" CornerRadius="10">
                        <Button BackgroundColor="Gray"/>
                    </Frame>
                    <!--Roller-->
                    <Grid Grid.Column="1" BackgroundColor="LightGray" Margin="2,0,2,0" RowSpacing="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Button Grid.Row="0" BackgroundColor="DarkGray"/>
                        <Button Grid.Row="1" BackgroundColor="DarkGray"/>
                    </Grid>
                    <!--Right-->
                    <Frame Grid.Column="2" OutlineColor="Black" BackgroundColor="Gray" Padding="1" HasShadow="True" CornerRadius="10">
                        <Button BackgroundColor="Gray"/>
                    </Frame>
                </Grid>
                <local:MovingSensor x:Name="MoveGrid" Grid.Row="1" BackgroundColor="LightGray"/>
                <!--The On button-->
                <Grid Grid.Row="2" BackgroundColor="LightGray">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Frame Grid.Row="0" OutlineColor="Black" BackgroundColor="Gray" HorizontalOptions="Center" Padding="1" HasShadow="True">
                        <Button Text="On" BackgroundColor="#4D94DB" Clicked="Button_Clicked"/>
                    </Frame>
                    <Label Text="{Binding Conn}" Grid.Row="1" HorizontalOptions="Center" VerticalOptions="CenterAndExpand"/>
                </Grid>
            </Grid>
        </Frame>
    </Grid>

</ContentPage>
